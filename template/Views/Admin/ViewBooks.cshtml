
@{
    ViewData["Title"] = "ViewBooks";
    Layout = "~/Shared/Admin/_adminLayout.cshtml";
}
<div>
@* @foreach (System.Data.DataRow dr in ViewBag.user_data.Rows)
{
    <tr>
        <td>@dr["BookName"].ToString()</td>
        <td>@Html.ActionLink("Delete", "delete", new { id = dr["id"].ToString() })</td>
    </tr>
}

@foreach (var imageUrl in ViewBag.ImageUrls)
{
    <p>@imageUrl</p>
    <img src="@imageUrl" alt="Image" />
} *@
 @*    @foreach (System.Data.DataRow dr in ViewBag.user_data.Rows)
    {
        <tr>
            <td>
                @dr["BookName"].ToString()
                <p>@ViewBag.ImageUrls[ViewBag.user_data.Rows.IndexOf(dr)]</p>
                <img src="@ViewBag.ImageUrls[ViewBag.user_data.Rows.IndexOf(dr)]" alt="Image" />
            </td>
        </tr>
    } *@
    @{
        var uniqueBooks = new HashSet<string>();
    }
</div>
<div id="content-page" class="content-page">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12">
                <div class="iq-card">
                    <div class="iq-card-header d-flex justify-content-between">
                        <div class="iq-header-title">
                            <h4 class="card-title">book Lists</h4>
                        </div>
                        <div class="iq-card-header-toolbar d-flex align-items-center">
                            <a asp-action="AddBooks" asp-controller="Admin" class="btn btn-primary">Add New book</a>
                        </div>
                    </div>
                    <div class="iq-card-body">
                        <div class="table-responsive">
                            <table class="data-tables table table-striped table-bordered" style="width:100%">
                                <thead>
                                    <tr>
                                        <th style="width: 3%;">No</th>
                                        <th style="width: 12%;">Book Image</th>
                                        <th style="width: 15%;">Book Name</th>
                                        <th style="width: 15%;">Book Catrgory</th>
                                        <th style="width: 18%;">Book Author</th>
                                        <th style="width: 22%;">Book Description</th>
                                        <th style="width: 7%;">Book Price</th>
                                        <th style="width: 15%;">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (System.Data.DataRow dr in ViewBag.user_data.Rows)
                                    {
                                        var books = dr["BookName"].ToString();

                                        if (!uniqueBooks.Contains(books))
                                        {
                                            uniqueBooks.Add(books);

                                    <tr>
                                            @* <td>@dr["BookName"].ToString()</td> *@
                                            <td>@dr["id"]</td>
                                            <td><img class="img-fluid rounded" src="@ViewBag.ImageUrls[ViewBag.user_data.Rows.IndexOf(dr)]" alt="Book Image"></td>
                                            <td>@dr["BookName"].ToString()</td>
                                            <td>@dr["BookCategory"].ToString()</td>
                                            <td>@dr["BookAuthor"].ToString()</td>
                                        <td>
                                                <p class="mb-0">@dr["BookDescription"].ToString()</p>
                                        </td>
                                            <td>@dr["BookPrice"].ToString()</td>
                                        <td>
                                            <div class="flex align-items-center list-user-action">
                                                    <a class="bg-primary" data-toggle="tooltip" data-placement="top" title="" data-original-title="Edit" asp-controller="Admin" asp-action="UpdateBookData" asp-route-id="@dr["id"]">
                                                        <i class="ri-pencil-line"></i>
                                            </a>
                                                    <a class="bg-primary" data-toggle="tooltip" data-placement="top" title="Delete" data-original-title="Delete" href="@Url.Action("deleteBookData", "Admin", new { id = dr["id"] })">
                                                        <i class="ri-delete-bin-line"></i>
                                                    </a>

                                            </div>
                                        </td>
                                    </tr>
                                  }
                                  }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
      </div>

